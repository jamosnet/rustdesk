name: Build RustDesk 1.4.0 IPA (Final V9)

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: Build iOS
    runs-on: macos-latest
    
    env:
      CC: /opt/homebrew/opt/llvm/bin/clang
      CXX: /opt/homebrew/opt/llvm/bin/clang++
      LDFLAGS: "-L/opt/homebrew/opt/llvm/lib"
      CPPFLAGS: "-I/opt/homebrew/opt/llvm/include"
      FLUTTER_STORAGE_BASE_URL: https://storage.googleapis.com

    steps:
      # 1. 拉取代码
      - name: Checkout Source Code (v1.4.0)
        uses: actions/checkout@v4
        with:
          ref: 1.4.0
          submodules: recursive

      # 2. Setup Rust (1.81.0 + rustfmt)
      - name: Setup Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.81.0
          components: rustfmt

      # 3. Setup Flutter (3.13.9 - 适配 Dart 3.1)
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.13.9'
          channel: 'stable'
          cache: true

      # 4. 安装系统依赖 (顺便装上 PyYAML)
      - name: Install System Dependencies
        run: |
          brew install llvm yasm
          echo "/opt/homebrew/opt/llvm/bin" >> $GITHUB_PATH
          pip3 install --upgrade pip setuptools --break-system-packages
          # 安装 PyYAML 用于修改配置文件
          pip3 install pyyaml --break-system-packages

      # 5. 手动创建缺失的源码文件
      - name: Mock Missing Source Files
        run: |
          echo "⚠️ Creating dummy files..."
          echo 'pub const VERSION: &str = "1.4.0";' > src/version.rs
          if [ ! -f "src/inline.rs" ]; then echo '// Dummy inline' > src/inline.rs; fi
          touch src/bridge_generated.rs

      # 6. 安装桥接工具
      - name: Install Bridge Codegen
        run: |
          cargo install flutter_rust_bridge_codegen --version 1.80.1 --features uuid --locked

      # 7. 【核心修复】智能修改 pubspec.yaml (使用 Python)
      # 避免 "Duplicate mapping key" 报错
      - name: Patch Dependencies (Smart)
        working-directory: ./flutter
        run: |
          echo "🔧 Patching pubspec.yaml using Python..."
          python3 -c "
          import yaml
          import os

          file_path = 'pubspec.yaml'
          with open(file_path, 'r') as f:
              data = yaml.safe_load(f) or {}

          # 确保 dependency_overrides 存在
          if 'dependency_overrides' not in data:
              data['dependency_overrides'] = {}

          # 强制锁定 extended_text 版本，解决 Dart SDK 冲突
          print('Overriding extended_text to 10.1.0...')
          data['dependency_overrides']['extended_text'] = '10.1.0'

          # 写回文件
          with open(file_path, 'w') as f:
              yaml.dump(data, f, default_flow_style=False)
          "
          
          echo "✅ pubspec.yaml patched successfully!"

      # 8. 安装 Flutter 依赖 (现在应该完美了)
      - name: Install Flutter Dependencies
        working-directory: ./flutter
        run: |
          echo "📦 Installing flutter dependencies..."
          flutter pub get

      # 9. 生成桥接文件 (Bridge)
      - name: Generate Bridge (Manual)
        run: |
          export PATH="$HOME/.cargo/bin:/opt/homebrew/opt/llvm/bin:$PATH"
          
          # 运行生成命令
          flutter_rust_bridge_codegen \
            --rust-input src/flutter_ffi.rs \
            --dart-output flutter/lib/generated_bridge.dart \
            --c-output flutter/ios/Runner/bridge_generated.h \
            --class-name RustdeskImpl
          
          if [ -f "flutter/lib/generated_bridge.dart" ]; then
            echo "✅ Bridge generated successfully!"
          else
            echo "❌ Bridge generation failed!"
            exit 1
          fi

      # 10. 编译 IPA (无签名)
      - name: Build IPA (No Codesign)
        working-directory: ./flutter
        run: |
          export PATH="$HOME/.cargo/bin:$PATH"
          flutter build ios --release --no-codesign

      # 11. 打包
      - name: Package into IPA
        run: |
          mkdir Payload
          APP_PATH=$(find flutter/build/ios -name "Runner.app" | head -n 1)
          if [ -z "$APP_PATH" ]; then
             APP_PATH=$(find . -name "Runner.app" | head -n 1)
          fi
          
          if [ -z "$APP_PATH" ]; then
            echo "Error: Runner.app not found! Build failed."
            exit 1
          fi
          
          echo "Packaging from: $APP_PATH"
          cp -r "$APP_PATH" Payload/
          zip -r RustDesk_1.4.0_Unsigned.ipa Payload

      # 12. 上传
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: RustDesk-1.4.0-Unsigned-IPA
          path: RustDesk_1.4.0_Unsigned.ipa
          retention-days: 3
